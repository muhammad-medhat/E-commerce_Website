import React from 'react';
import './subheading.css'

const Subheading = ({title}) => {
    return (
        <h4 className="category__wrapper_head text-uppercase mb-5">{title}</h4>
    )
}

export default Subheading